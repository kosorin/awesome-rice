{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run with Xephyr",
            "type": "lua-local",
            "request": "launch",
            "program": {
                "command": "${workspaceFolder}/scripts/run.sh",
            },
            "args": [
                "--config",
                "${workspaceFolder}/rc.lua",
                "--bin",
                "/usr/bin/awesome",
                "--lib",
                "/usr/share/awesome/lib",
                "--lib",
                "${workspaceFolder}",
            ],
        },
    ],
}